---
title: chirpy 테마 이미지 넣기 변경됨 (feat. vscode)
date: 2026-05-02 16:30:00 +09:00
categories: [github.io, vscode]
tags: [github.io]
pin: true
---

## 이미지 변경?

평소처럼 블로그를 작성하고 업로드 했는데 둘러보다 이미지를 클릭했더니 되게 이상하게 뜨는 것을 발견했다.

![Image](https://github.com/user-attachments/assets/064b5cad-2751-4cf6-bd87-0bba4cccaeb4){: width="1919" height="898" }

위처럼 무슨 스크롤이 생겨 이미지가 잘리는 문제가 있었다. 분명 이전 버전까지만 해도 잘 됬는데!!

아무래도 버전을 올리면서 뭔가 변경 사항이 있었던 듯 싶다.

![Image](https://github.com/user-attachments/assets/f12f2426-6719-4d72-9b1f-b8984aa89356){: width="725" height="504" }

그래서 찾아보니 이거 원... 버전 이슈가 맞더라.. 이전게 좋았는데!!

## 해결 방법

그럼 어떻게 해결하는고? 하니 1번으론 직접 코드를 뜯어 고치는게 있고 2번으론 그냥 이전까지 **md**로 작성할 때 vscode 에서 `바로가기 키 설정(json)`을 편집했는데 그걸 다시 바꿔야 하는 상황이 왔다...

근데 머.. 깡통이들은 추천하길 가능하면 **width**, **height**를 넣으라 하니 그리 해줘보자.

![Image](https://github.com/user-attachments/assets/0102f7dc-6ba4-4085-aed3-27b10ea05608){: width="718" height="436" }

참고로 바로가기 키 설정 검색할 때 **ctrl+shift+p**를 눌러 검색하면 된다.

```json
// 키 바인딩을 이 파일에 넣어서 기본값 재정의
[
    {
        "key": "ctrl+shift+v",
        "command": "editor.action.insertSnippet",
        "when": "editorTextFocus && editorLangId == 'markdown'",
        "args": {
            "snippet": "![${1}](${CLIPBOARD/.*src=\"([^\"]+)\".*/$1/})"
        }
    }
]
```

그리고 이전까지는 위처럼 이미지를 github에 복사해 넣고 나온 `<img width="718" height="436" alt="Image" src="https://github.com/user-attachments/assets/0102f7dc-6ba4-4085-aed3-27b10ea05608" />` 이 링크를 복사해 **ctrl+shift+v**를 했을 때 자동으로 `![]()` 구조가 될 수 있게 만들었었다.

근데 이제 저기에 추가로 **width**, **height**를 추가해줘야하는 상황.. 

![Image](https://github.com/user-attachments/assets/5669643d-dd44-44ab-aa2c-0052df6c563d){: width="1538" height="354" }

그렇기에 조금 길긴 하지만 

```json
// 키 바인딩을 이 파일에 넣어서 기본값 재정의
[
  {
    "key": "ctrl+shift+v",
    "command": "editor.action.insertSnippet",
    "when": "editorTextFocus && editorLangId == 'markdown'",
    "args": {
      "snippet": "${CLIPBOARD/<img.*width=\"(\\d+)\".*height=\"(\\d+)\".*alt=\"([^\"]*)\".*src=\"([^\"]+)\".*/![$3]($4){: width=\"$1\" height=\"$2\" }/}$0",
    },
  },
]
```

이렇게 이름과 url, width, height를 모두 넣어주는 코드를 작성할 수 있었다.

혹시 이건 이름이 다 image가 되니까 싫다! 하면

```json
[
    {
        "key": "ctrl+shift+v",
        "command": "editor.action.insertSnippet",
        "when": "editorTextFocus && editorLangId == 'markdown'",
        "args": {
            "snippet": "![${1:대체 텍스트 입력}](${CLIPBOARD/.*src=\\s*\"([^\"]+)\".*/$1/}){: width=\"${CLIPBOARD/.*width=\\s*\"(\\d+)\".*/$1/}\" height=\"${CLIPBOARD/.*height=\\s*\"(\\d+)\".*/$1/}\" }$0"
        }
    }
]
```

이전과 비슷한 이걸 쓰면 된다. 붙여넣기를 하면 바로 이름 편집이 가능할거다. 난 아무래도 마지막게 가장 편해서 이걸로 쓸 듯 하다.